export enum PlantStatus {
  ACTIVE = 'active',
  DEAD = 'dead',
  GIFTED = 'gifted',
}

export enum PostType {
  UPDATE = 'update',
  SWAP = 'swap',
  GIVEAWAY = 'giveaway',
}

export enum UserRole {
  USER = 'user',
  ADMIN = 'admin',
}
